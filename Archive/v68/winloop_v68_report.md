# WinLoop Continuation — V68

**Status:** validated continuation from committed V67. V68 binds to V67 validation digest `972ae30b001ead74b4ec3dff9fa9239343618949218a4551d43d0596209f1162` and implementation SHA-256 `91096f108d4a7bd96fc49b506d68757badf6740e8d9d87b4cb449bb09d150248`, preserves V21 guarded routing, and keeps the carried admission baseline at joint 21 / provenance 22 / synthetic lower 63.

## Independence evidence remains fail closed

No committed independently validated provider/operator/hardware independence certificate exists in the canonical repository state examined for this continuation, so V68 keeps cross-role credit at **12**. The executable gate evaluates **150** certificate/anchor/relation combinations and admits only **4** hypothetical states requiring current or canonically cached external evidence, a current/cached independent anchor, and an explicitly disjoint external relation. There are zero stale/conflicting, alias/unknown-relation, or self-asserted acceptances. Signed metadata and modeled role labels remain insufficient to prove physical, organizational, hardware, supply-chain, cloud-control, or operator independence.

## Epoch-19 anchor GC through total proof-source disappearance and cold verifier restart

V68 extends the compaction model through epoch 19. `T18 -> CP19` preserves the original epoch-12 freshness origin even when every previously modeled proof source disappears and a verifier subsequently cold-starts. Total-source-loss recovery requires a pre-disappearance pinned authority root and the canonical cached latest-history form; cold restart is a bridge only and cannot mint a new root or reset the deadline. With every proof source absent, a live epoch-19 clear is rejected; only an already-bound cached clear can survive against the pinned pre-disappearance anchor.

The exact matrix contains **215,796,940,800** history/compaction/source/revocation/verifier/publication/anchor/delay states, of which **408,240** admit. The accepted set contains **3,600** complete-source-disappearance recoveries, **26,280** cold-restart recoveries, **1,800** combined disappearance-plus-cold-restart recoveries, **720** cached epoch-19 clear recoveries after total source loss, and **42,720** source-reappearance recoveries. The shared end-to-end freshness budget remains **3 steps** from epoch 12 across seven modeled propagation legs. There are **zero** post-deadline acceptances, **zero** deadline-reset acceptances, **zero** stale/fork-clear acceptances, **zero** cold-restart-as-authority acceptances, **zero** unpinned-total-loss acceptances, and **zero** fork acceptances.

## Two proof-source losses composed with simultaneous publication-root rollback

V68 composes the V67 two-proof-source-loss path with a simultaneous rollback of both one verifier publication view and the authority-root view. Acceptance still requires a 2-of-3 post-transition publication quorum. A rollback is admissible only as one bound event: the rollback publication view, explicit rollback bridge, pre-rollback pinned authority root, and rollback-root record must agree. When both proof sources are also lost, the proof form must be dual-attested before loss. Cached proof data can preserve evidence but cannot become the authority that validates itself, and a rollback root without its corresponding publication rollback is rejected.

The exact matrix contains **2,275,983,360** publication/evidence/source/join/bridge/authority/root/delay states, of which **521,640** admit. It includes **27,720** two-proof-source-loss recoveries, **10,920** publication-rollback recoveries, **840** simultaneous two-source-loss plus publication-root-rollback recoveries, and **123,480** replacement-source recoveries. There are **zero** cached-authority-promotion acceptances, **zero** unbound-root-rollback acceptances, **zero** fork acceptances, **zero** stale/missing-proof acceptances, **zero** untrusted/conflicting-join acceptances, and **zero** post-deadline acceptances.

## Third Byzantine eviction after rollback recovery under one honest verifier loss

V68 extends the carried 3-of-5 membership model beyond V67's second eviction and join rollback. The third join and third eviction are accepted only on an evidence chain that explicitly crosses rollback recovery: rollback bridge -> rollback-recovery certificate -> join-3 certificate -> eviction-3 certificate. The model treats prior evictions as certificate-bound history rather than requiring every tombstoned identity to occupy a current population slot; current trusted membership still must satisfy the full 3-of-5 threshold. A single honest verifier population may be unavailable, but the threshold is never reduced and an active Byzantine population never contributes to trusted membership.

The exact matrix contains **38,698,352,640** population/phase/history/evidence/verifier/join/delay states, of which **684,390** admit. It includes **38,850** rollback-recovery states, **75,600** third-join recoveries, **75,600** third-eviction recoveries, and **50,400** third-eviction recoveries while one honest verifier population is unavailable. There are **zero** rollback-chain-bypass acceptances, **zero** replacement-self-authorization acceptances, **zero** active-Byzantine acceptances after eviction, **zero** two-honest-verifier-loss acceptances, **zero** untrusted/conflicting-join acceptances, **zero** membership/eviction-fork acceptances, **zero** below-threshold-history acceptances, and **zero** post-deadline acceptances.

## Preserved bounds and evidence ceiling

The V66-carried temporal regression remains unchanged: horizon 22 floor **1 / synthetic budget 851**, horizon 11 floor **2 / budget 398**. These remain synthetic stage-rate model parameters rather than empirical attacker prices or response times. The 513-statement checkpoint-recovery bound, maximum lag 64, frontier-only Merkle storage, unchanged trust-bearing message paths, shared-audit accounting `132 + 4*k`, V21 routing, and no-new-runtime-envelope claim remain unchanged. Unknown, stale, conflicting, cyclic, aliased, self-asserted, unbound, cache-promoted, or cold-restart-promoted provenance fails closed.

## V69 frontier

V69 should continue to require committed independently validated provider/operator/hardware evidence before any cross-role-credit increase; extend anchor GC through epoch 20 across total source loss followed by source reappearance and a second cold verifier restart; compose publication-root rollback with verifier-population rollback and delayed join evidence without cached-authority promotion; and test a fourth Byzantine eviction with replacement-identity recycling after the rollback-bound third eviction. V21 routing remains active unless a replacement independently clears the >=2,000-seed acceptance bar with honest message accounting.
