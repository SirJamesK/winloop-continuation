# WinLoop GitHub Continuation — Scheduled Task Prompt

Continue developing and validating WinLoop from the latest preserved V42 state.

## Preserve the V42 architecture contract

- The carried V13 endpoint theorem is a baseline only; do not claim to re-prove it unless a fresh independent certificate is produced.
- The V21 guarded routing design remains the active routing baseline unless a replacement independently clears its acceptance bar.
- Admission requires:
  - joint false-WIN cut >= 21;
  - synthetic lower cost >= 60;
  - every non-endpoint route cut >= 22.
- V42 accepted deep/common privileged fabric requires independent fabric-local/current authorization and rejects provenance cut 21.
- Temporal modeling must treat verifier-visible stolen-authorization lifetime as the operative variable.
- Recursive evidence must fail closed on unknown, stale, conflicting, or insufficient dependencies.
- Merkle frontier/root persistence is a storage optimization only.
- Shared-audit accounting remains `132 + 4*k` messages per audit epoch unless a stronger honestly-counted design is proven.

## GitHub source-of-truth policy

1. Locate the dedicated GitHub repository exactly named `SirJamesK/winloop-continuation` through the connected GitHub app.
2. **Never write to any other repository.** If that repository is unavailable or inaccessible, report only the blocker and stop the run.
3. Inspect its default branch. Write directly only when the default branch is `main` or `master`.
4. Never create feature branches, release branches, or pull requests for scheduled WinLoop continuation work.
5. Prefer `main` for a newly initialized repository.
6. Continue from the latest version already committed in the repo, not from an older chat artifact.
7. Generate and validate the next integrated implementation, validation JSON, report, standalone validator, SHA-256 manifest, and reproducibility/support files.
8. Update canonical latest files on the default branch and add a concise immutable run record under `runs/<version>/` or equivalent versioned path without unnecessary large duplication.
9. GitHub is the durable source of truth.
10. In the ChatGPT scheduled-task response, do not paste the full technical report. Return only: version, verified headline result, GitHub commit SHA or exact blocker, and link/reference if available.

Do not claim hidden infrastructure access, unrestricted networking, covert communication, model/runtime self-modification, or capabilities not actually exposed.
